{
  "name": "@defiant/risk",
  "version": "0.1.0",
  "private": true,
  "description": "Versioned, typed risk classifications (tier, protocol maturity, fee level, exit profile) with reasoning, not just labels.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
