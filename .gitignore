# dependencies — npm workspaces hoists everything to the repo root, but stay
# unanchored in case a workspace ever gets its own node_modules
node_modules

# next.js (apps/web)
.next/
out/

# production
build/

# package build output (packages/*/dist)
dist/

# env
.env
.env.local
.env*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# Local KYB / PII — never commit
kyb-local

# typescript
*.tsbuildinfo
next-env.d.ts
.vercel
